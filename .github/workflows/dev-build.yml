name: Dev Build

on:
  workflow_dispatch:
    inputs:
      module:
        description: Module to build
        required: true
        type: choice
        options:
          - client
          - server

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        name: Checkout main

      - uses: pnpm/action-setup@v4
        name: Setup pnpm
        with:
          version: 10.15.0

      - uses: actions/setup-node@v4
        name: Setup node
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test:${{github.event.inputs.module}}

      - name: Build
        run: pnpm build:${{ github.event.inputs.module }}
