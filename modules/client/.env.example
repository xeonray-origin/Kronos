# Client build-time public variables (inlined by dotenv-webpack).
# WARNING: values here are baked into the browser bundle — never put secrets in this file.

API_BASE_URL=http://localhost:8080

# Toggle the in-browser MSW mock. true = use mock; false = hit the real API at API_BASE_URL.
USE_MOCK=false
